describe('github app installation', () => {
  test('correctly handles new repositories', async () => {})

  test('correctly handles removed repositories', async () => {})
})

test('github repository push correctly handles changed repository', async () => {})
